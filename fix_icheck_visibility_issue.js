/**
 * Fix for iCheck Visibility Issue
 * 
 * This addresses the specific case where iCheck initializes (helper element is present)
 * but the actual checkbox is not visible due to CSS or positioning issues.
 */

(